const { readConstant } = require('../../constant')
module.exports = {
    // segementFault 显示长度
    [readConstant.READ_CONF_SEGE]: {
        length: 10
    },
    // 微博显示长度
    [readConstant.READ_CONF_BLOG]: {
        length: 10
    }
}
